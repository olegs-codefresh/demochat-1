version: '1.0'
steps:

  build_dev_step:
    type: build
    dockerfile: Dockerfile.dev
    image-name: demochatnode
    tag: dev

  build_step:
    type: build
    dockerfile: Dockerfile
    image-name: demochat
    tag: ${{CF_BRANCH}}
